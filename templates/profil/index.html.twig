{% extends 'base.html.twig' %}

{% block icon %}👤{% endblock %}
{% block title %}Profil{% endblock %}
{% block background %}{{ asset('img/background-info.png') }}{% endblock %}

{% block body %}
    <h1>Profil :</h1>
    <p>Nom : {{ app.user.firstName }}</p>
    <p>Prenom : {{ app.user.lastName }}</p>
    <p>Téléphone : {{ app.user.phone }}</p>
    <p>Adresse mail : {{ app.user.email }}</p>
    <p>Date de Naissance : {{ app.user.dateNais }}</p>
    {% if app.user.cv %}
        <p>CV enregistré : <a href="{{ asset('uploads/pdf/' ~ app.user.cv) }}">CV (PDF)</a></p>
    {% else %}
        <p>CV enregistré : Aucun</p>
    {% endif %}
    {% if app.user.lettreMotiv %}
        <p>Lettre de motivation enregistrée : <a href="{{ asset('uploads/pdf/' ~ app.user.lettreMotiv) }}">Lettre de motivation (PDF)</a></p>
    {% else %}
        <p>Lettre de motivation enregistrée : Aucune</p>
    {% endif %}
    <a href="/profil/modif"><button type="button" href="/profil/modif">Modifier mon profil</button></a>
{% endblock %}
