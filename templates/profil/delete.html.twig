{% extends 'base.html.twig' %}

{% block icon %}👤{% endblock %}
{% block title %}Suppression du Compte{% endblock %}
{% block background %}{{ asset('img/background.png') }}{% endblock %}

{% block body %}

    <form class="login_bloc" method="post">
        <div class="login_form">
            <h1 class="h3 mb-3 font-weight-normal">SUPPRESSION DU COMPTE</h1>

            {{ form_start(form) }}
            <div>{{ form_row(form.Delete, {'attr': {'class': 'btn btn-primary'}}) }}</div>
            <div>{{ form_row(form.Cancel, {'attr': {'class': 'btn btn-secondary'}}) }}</div>
            {{ form_end(form) }}
        </div>
    </form>
{% endblock %}
